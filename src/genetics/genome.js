import {GENE} from '../core/constants.js';
const C=x=>Math.max(0,Math.min(1,x));
export function randomGenome(rng){const g=new Float32Array(GENE.COUNT);for(let i=0;i<g.length;i++)g[i]=C(.5+rng.gaussian()*.17);return g}
export function inheritGenome(a,b,rng){const g=new Float32Array(GENE.COUNT);for(let i=0;i<g.length;i++)g[i]=C((a[i]+b[i])*.5+rng.gaussian()*.05);return g}
export function derived(genome,age,nutrition=1){const prime=age<16?.48+age/32:age<45?1:Math.max(.42,1-(age-45)/70),senseAge=age<40?1:Math.max(.45,1-(age-40)/75);return{strength:(.45+genome[GENE.STRENGTH]*.75)*prime*nutrition,endurance:(.55+genome[GENE.ENDURANCE]*.65)*prime,speed:(.55+genome[GENE.SPEED]*.75)*prime,memory:150+Math.round(genome[GENE.MEMORY]*250),perception:4+Math.round(genome[GENE.PERCEPTION]*7),longPerception:(25+genome[GENE.PERCEPTION]*15)*senseAge,hearing:(.55+genome[GENE.HEARING]*.8)*senseAge,smell:(.45+genome[GENE.SMELL]*.9)*senseAge,taste:.45+genome[GENE.DISEASE]*.35+genome[GENE.PERCEPTION]*.2,painTolerance:.35+genome[GENE.ENDURANCE]*.35+genome[GENE.STUBBORN]*.3,learning:.65+genome[GENE.LEARNING]*.9,longevity:52+Math.round(genome[GENE.LONGEVITY]*48)}}
export const traitNames=[['força',GENE.STRENGTH],['aprendizado',GENE.LEARNING],['memória',GENE.MEMORY],['visão',GENE.PERCEPTION],['audição',GENE.HEARING],['olfato',GENE.SMELL],['agressividade',GENE.AGGRESSION],['sociabilidade',GENE.SOCIABILITY],['ambição',GENE.AMBITION],['cautela',GENE.CAUTION],['lealdade',GENE.LOYALTY],['ganância',GENE.GREED],['curiosidade',GENE.CURIOSITY],['teimosia',GENE.STUBBORN],['empatia',GENE.EMPATHY]];
