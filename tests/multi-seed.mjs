import {Simulation} from '../src/simulation.js';
import {DAY_TICKS,GENE} from '../src/core/constants.js';
const count=+(process.env.SEEDS||10),days=+(process.env.DAYS||300),seeds=Array.from({length:count},(_,i)=>[42,7919,1337,2026,77,909,31415,8888,1201,6543][i]??(1000+i*7919));const rows=[];for(const seed of seeds){const s=new Simulation(seed);for(let i=0;i<DAY_TICKS*days;i++)s.step();const alive=s.npcs.living();const mean=g=>alive.length?alive.reduce((z,i)=>z+s.npcs.gene(i,g),0)/alive.length:0;rows.push({seed,alive:alive.length,created:s.npcs.nextUid-1,boss:!s.dungeon.boss.alive,day:s.meta().day,wood:s.actionHistogram['cortar madeira']||0,farm:s.actionHistogram['cultivar']||0,travel:+(s.travel.completed/Math.max(1,s.travel.started)).toFixed(3),genes:[GENE.AGGRESSION,GENE.CAUTION,GENE.AMBITION,GENE.EMPATHY].map(mean)})}console.table(rows);const extinct=rows.filter(r=>r.alive===0).length;if(extinct)process.exitCode=1;
